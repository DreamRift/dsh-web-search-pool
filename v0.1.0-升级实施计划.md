# AI搜索 Key 池负载均衡 v0.1.0-rc.7 升级实施计划

## 设计依据
- 规范文档：docs/superpowers/specs/2026-08-18-native-bundle-rc7-design.md
- 当前版本：v0.2.0 → 目标版本：v0.1.0-rc.7
- 宿主版本：DeepSeek Harness 0.1.0-rc.7

## 必须完成的变化 (v0.2.0 → v0.1.0-rc.7)

### 1️⃣ Package.json 升级
- [x] ✅ 新增 `dsh.bundle.patch` 字段指向 `cordis.patch.yml`
- [ ] 更新 `peerDependencies` 为精确 rc.7 版本（不能是 `*`）
- [ ] 在 `files` 数组中添加 `cordis.patch.yml`

### 2️⃣ Bundle Patch (cordis.patch.yml)
- [ ] ✅ 新增文件：覆盖 web row 的 `searchProvider: search-pool`
- [ ] ✅ 插入 `web-search-pool` provider row

### 3️⃣ Client half 修复
- [ ] ⚠️ **CRITICAL** 修改 client.js keyed slot 注册
  - ❌ 旧：id: 'search-pool'  
  - ✅ 新：**key: 'web-search-pool'** (与 namespace 一致)
- [ ] ✅ 清理 timer/subscription 泄漏

### 4️⃣ Legacy Script 调整
- [ ] 📝 patch-api-proxy-namespace.mjs 改为 rc.7 诊断模式
  - rc.7 不再执行此脚本
  - 检测后输出"无需 patch，已自动暴露"提示
  - 保留 legacy 兼容以支持旧版 rc.6

### 5️⃣ 新增测试 (TDD)
- [ ] bundle-contract.test.js：验证 bundle patch 结构
- [ ] client-contract.test.js：验证 keyed slot 注册格式
- [ ] install-contract.test.js：验证 pack 内容和 peer 依赖

### 6️⃣ 文档更新
- [ ] docs/挂载指南.md → 重写为 rc.7 原生安装方式
- [ ] docs/升级与回滚指南.md → 新增
- [ ] docs/开发事故复盘与规范.md → 补充 keyed slot 规则

## 关键问题诊断

### 🔴 Problem 1: Client 使用错误的 slot key
**现象**: client.js 中使用 `id: 'search-pool'`，但 rc.7 要求 `key: 'web-search-pool'`

**原因**: rc.6 允许 id，但 rc.7 keyed slot 必须使用严格匹配的 key

**修复**: 立即修改 src/dsh/client.js apply() 函数中的 slots.register

### 🟡 Problem 2: peerDependencies 版本不精确
**现状**: peer dependencies 全部使用 `*`

**风险**: npm 可能解析到 rc.1/rc.6，造成版本冲突

**修复**: 全部指定为 `^0.1.0-rc.7`

### 🟢 Problem 3: 缺少 bundle contract
**现状**: 没有自动化验证 cordis.patch.yml 结构的测试

**修复**: 新增 bundle-contract.test.js

## 执行顺序

### Phase 1: 核心改造 (Blocker)
1. 修改 client.js keyed slot (fix critical bug)
2. 更新 package.json peer deps
3. 创建 cordis.patch.yml
4. 运行现有测试确保无破坏性变更

### Phase 2: TDD 建设
5. 新增三个 contract tests
6. 修正任何失败点
7. 运行 npm pack --dry-run

### Phase 3: 文档发布
8. 重写安装文档
9. 编写升级指南
10. 更新事故规范

## 风险控制

- **回滚策略**: 保持 src/向后兼容，只改 metadata + bundle + client
- **兼容性**: patch-api-proxy-namespace.mjs 保留 legacy 路径
- **测试验证**: 所有修改前先建立 FAILING test，确认修复后变为 PASSING

## 验收标准

✅ 满足 design doc 第 21 节所有 acceptance criteria
✅ npm pack --dry-run 成功且包内容正确
✅ 通过真实 profile 安装验证（可选环境阻塞时记录限制）
